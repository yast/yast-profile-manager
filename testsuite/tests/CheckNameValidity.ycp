/**
 * Author:	Jiri Suchomel <jsuchome@suse.cz>
 * $Id$
 */
{
    // testedfiles: ProfileManager.ycp
    import "Testsuite";
    import "ProfileManager";

    define void TEST_It_Now (string name) ``{

        Testsuite::Dump("name to check:");
        Testsuite::Dump(name);
	Testsuite::Test (``(ProfileManager::CheckNameValidity(name)), [ $[], $[], $[] ], nil);
    }

    TEST_It_Now ("new name");
    TEST_It_Now ("new_name");
    TEST_It_Now ("_new_name");
}
