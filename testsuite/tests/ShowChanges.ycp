{

    include "testsuite.ycp";

    import "ProfileManager";


    map EX_true =
    $[ "scpm":
        $[ "profiles" :
            $[ "changes": true ]
        ]
    ];

    map EX_false =
    $[ "scpm":
        $[ "profiles" :
            $[ "changes": false ]
        ]
    ];

    map READ_ok =
    $[ "target":
		$[ "string": "Changes" ]
	];

    map READ_nil =
    $[ "target":
		$[ "string": nil ]
	];

    ProfileManager::changespath = "/tmp/changes.out";

    ProfileManager::switch_info =
    $[ "profile_name": "home",
       "profile_modified": true,
       "modified_resources":
       [ $[ "resource_name": "/etc/X11/XF86Config",
            "resource_type": "file",
            "is_new": false,
            "is_deleted": true,
            "save": true
         ],
         $[ "resource_name": "sendmail",
            "resource_type": "service",
            "is_new": false,
            "is_deleted": true,
            "save": false
         ],
       ]
    ];

    DUMP(ProfileManager::switch_info);
    DUMP("---------");

    ProfileManager::active_resource = 0;
	TEST (``(ProfileManager::ShowChanges()), [ READ_nil, $[], EX_true ], nil);
    DUMP("---------");

	TEST (``(ProfileManager::ShowChanges()), [ READ_ok, $[], EX_true ], nil);
    DUMP("---------");

    ProfileManager::active_resource = 1;
	TEST (``(ProfileManager::ShowChanges()), [ READ_ok, $[], EX_true ], nil);

}
