/**
 * File:
 *   cfg_scpm.scr
 * Summary:
 *   SCR Agent for reading/writing /etc/scpm.conf
 * Access:
 *   read/write
  * Example:
 *   Dir(.scpm)
 *   (["DEBUG", "VERBOSE", "SWITCH_MODE", "RESOURCE_SET", "BOOT_MODE"])
 **
 *   Read(.scpm.VERBOSE)
 *   ("yes")
 **
 *   Write(.scpm.VERBOSE, "no")
 *   (true)
 *
 * $Id$
 *
 * Read/Sets the values defined in <tt>/etc/scpm.conf</tt>
 * in an easy manner.
 */
.sysconfig.scpm

`ag_ini(
  `SysConfigFile("/etc/scpm.conf")
)
