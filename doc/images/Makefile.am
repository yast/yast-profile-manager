#
# Makefile.am for yast2-profile-manager/doc/images
#

htmldir = $(docdir)/images

doc_DATA = 
html_DATA = profile-manager.png
#we do not need to include screenshots in package

EXTRA_DIST = $(doc_DATA) $(html_DATA)
